---
- name: Wait for SSH to be ready
  wait_for:
    port: 22
    host: "{{ ansible_host | default(inventory_hostname) }}"
    search_regex: OpenSSH
    delay: 10
    timeout: 300
  connection: local
